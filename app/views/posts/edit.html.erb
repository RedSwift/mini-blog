<h1>Posts#edit</h1>
<%= form_for [current_user, @post] do |p| %>
  <%= p.label :title %> <br>
  <%= p.text_field :title %>
  <br>
  <%= p.label :body %> <br>
  <%= p.text_area :body %>
  <br>
  <%= p.submit "Update Post" %>
<% end %>

<%= link_to "Back", user_posts_path %>

<h1>New Post</h1>

<%= form_for [current_user, @post] do |p| %>
  <%= p.label :title %> <br>
  <%= p.text_field :title %>
  <br>
  <%= p.label :body %> <br>
  <%= p.text_area :body %>
  <br>
  <%= p.submit "Create Post" %>
<% end %>

<%= link_to "Back", user_posts_path %>
